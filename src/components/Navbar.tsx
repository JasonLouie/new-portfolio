import Button from "./Button";

export default function Navbar() {
    const navElementClasses = ``;
    return (
        <nav>
            <Button href="/" id="home" className={navElementClasses}>Home</Button>
        </nav>
    );
}